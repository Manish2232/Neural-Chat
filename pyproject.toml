[project]
name = "neural-chat"
version = "0.1.0"
description = "Neural Chat AI"
readme = "README.md"
requires-python = ">=3.11"

dependencies = [
    "streamlit",
    "python-dotenv",
    "langchain",
    "langchain-core",
    "langchain-community",
    "langchain-mistralai",
    "langgraph",
    "requests",
    "httpx",
    "pydantic"
]

[tool.uv]
package = false